diff --git a/cups/snmp.c b/cups/snmp.c
index 54e348f1..c9c2c480 100644
--- a/cups/snmp.c
+++ b/cups/snmp.c
@@ -1042,10 +1042,13 @@ asn1_decode_snmp(unsigned char *buffer,	/* I - Buffer */
 	        case CUPS_ASN1_OCTET_STRING :
 	        case CUPS_ASN1_BIT_STRING :
 	        case CUPS_ASN1_HEX_STRING :
-		    packet->object_value.string.num_bytes = length;
 		    asn1_get_string(&bufptr, bufend, length,
 		                    (char *)packet->object_value.string.bytes,
 				    sizeof(packet->object_value.string.bytes));
+        unsigned copy_len = length;
+        if (length > sizeof(packet->object_value.string.bytes) - 1)
+          copy_len = sizeof(packet->object_value.string.bytes) - 1;
+        packet->object_value.string.num_bytes = copy_len;
 	            break;
 
 	        case CUPS_ASN1_OID :
